package fileTest;

import java.io.File;

public class Test {
	public static void main(String[] args) {
		String rootPath = "C:/file/";

		File directory = new File(rootPath);
        if(!directory.exists()){
            directory.mkdirs();
        }

	}
}
